# Re-apply local patches to vendored git submodules on every activation -
# `git submodule update` resets them to pristine upstream, which doesn't
# carry local fixes. Safe to re-run (idempotent). See scripts/fix-sphincs-abi.sh
bash scripts/fix-sphincs-abi.sh

use flake
